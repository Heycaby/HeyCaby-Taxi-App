---
description: Prefer Cursor MCP tools for external services (Notion, Datadog, Supabase, etc.)
alwaysApply: true
---

# MCP first

When the task touches an external system and this workspace has a **configured MCP server** for it, **use MCP** (read tool schemas, then call tools) instead of improvising with curl, undocumented APIs, or guessing.

- **Before calling an MCP tool:** inspect the tool’s schema/descriptor so arguments match what the server expects.
- **If multiple approaches exist** (e.g. Supabase Dashboard vs CLI vs MCP): prefer **MCP** when it is connected and covers the operation.
- **If MCP is not available** for that system in this session, say so briefly and fall back to the repo’s documented workflow (e.g. `supabase` CLI, scripts in `scripts/`).

This matches the project owner preference: **always use MCP when it applies.**

## Supabase: MCP for push / deploy

**Always use the Supabase MCP** for database and backend deploys — do not use `supabase db push`, Dashboard SQL paste, or ad-hoc curl unless MCP is unavailable in the session.

| Operation | MCP tool |
|-----------|----------|
| Apply migrations | `apply_migration` (SQL from `supabase/migrations/`) |
| Ad-hoc SQL / smoke checks | `execute_sql` |
| Edge functions | `deploy_edge_function` |
| Logs / advisors | `get_logs`, `get_advisors` |

Read tool schemas with `GetMcpTools` before calling. After `apply_migration`, confirm with `execute_sql` on `supabase_migrations.schema_migrations` or a targeted smoke query.

## Supabase: production first

Device and agent testing use **HEYCABY-TAXI production**, not staging.

| Environment | Project | Ref / `project_id` |
|-------------|---------|-------------------|
| **Production (default for tests)** | HEYCABY-TAXI | `fvrprxguoternoxnyhoj` |
| Staging (legacy / optional) | HeyCaby Staging | `fdavszxncggswuiwggcp` |

- **Apply migrations, `execute_sql`, edge-function deploys, and smoke checks on production** unless the user explicitly asks for staging.
- Rider/driver `.env` / `--dart-define` must point at production (`https://fvrprxguoternoxnyhoj.supabase.co`) during QA and device testing.
- Staging-only webhook / `app_config` migrations must not be copied to production unchanged (see `supabase/README.md`).
