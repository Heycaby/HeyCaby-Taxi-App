import type { Metadata } from "next";
import { adminRpc } from "@/lib/admin";
import { dateTime, money, shortId } from "@/lib/format";
import { Empty, PageHeader, Section, Status } from "@/components/ui";
export const metadata:Metadata={title:"Payments"};
type Payment={id:string;ride_id:string;driver_id:string;provider:string;state:string;amount_cents:number;currency:string;platform_fee_cents:number;driver_route_cents:number;correlation_id:string;failure_code:string;paid_at:string;refunded_cents:number;routed_at:string;created_at:string};
export default async function PaymentsPage(){const rows=await adminRpc<Payment[]>("fn_admin_os_payments",{p_limit:100});return <><PageHeader eyebrow="Payment Service" title="Payments" description="Mollie lifecycle state, correlation IDs, commission snapshots, refunds, and routing. No browser-side payment confirmation."/><Section title="Prepaid payment journal" description="Latest 100 payment records">{rows.length===0?<Empty title="No prepaid payments yet" body="Rollout flags remain disabled until sandbox and production safety gates pass."/>:<div className="table-wrap"><table><thead><tr><th>Payment</th><th>Ride</th><th>State</th><th>Amount</th><th>Commission</th><th>Driver route</th><th>Correlation</th><th>Created</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td><strong>#{shortId(p.id)}</strong><small>{p.provider}{p.failure_code&&` · ${p.failure_code}`}</small></td><td>#{shortId(p.ride_id)}</td><td><Status value={p.state}/>{p.refunded_cents>0&&<small>{money(p.refunded_cents,p.currency)} refunded</small>}</td><td>{money(p.amount_cents,p.currency)}</td><td>{money(p.platform_fee_cents,p.currency)}</td><td>{money(p.driver_route_cents,p.currency)}</td><td><code>{shortId(p.correlation_id)}</code></td><td>{dateTime(p.created_at)}</td></tr>)}</tbody></table></div>}</Section></>}
