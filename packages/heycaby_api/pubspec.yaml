name: heycaby_api
description: HeyCaby shared API layer — HTTP client, Supabase client, secure storage

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0
  supabase_flutter: ^2.5.0
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.5
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
