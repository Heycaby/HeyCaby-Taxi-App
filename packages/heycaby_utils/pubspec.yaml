name: heycaby_utils
description: HeyCaby shared utilities — validators, formatters, error handling

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  device_info_plus: ^11.2.0
  flutter:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
